# Enlace simbólico local sin tildes (workaround bug de biber/Perl con rutas UTF-8 desde apps GUI)
/redaccion_tfg

# Datos crudos — no se versionan (informes empresariales pesados)
data/raw/
data/interim/
data/processed/

# data/external: todo se versiona excepto credenciales o datos de acceso restringido

# Los PDFs de normativa y diccionarios SÍ se versionan (documentos de referencia)
# data/external/normativa/ → versionado
# data/external/diccionarios/ → versionado

# Archivos Excel pesados
*.xlsx
*.xls

# Entorno Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/

# Variables de entorno y secretos
.env
.env.*

# VS Code
.vscode/

# macOS
.DS_Store

# Modelos grandes
results/models/*.pkl
results/models/*.bin
results/models/*.pt
results/models/*.npy

# Descartes de reemplazo de informes (backups locales, no versionar)
/_pendiente_reemplazo/
/_rechazados_ES/

# Auxiliares de compilación LaTeX (LaTeX Workshop)
**/build/
*.aux
*.bbl
*.bcf
*.blg
*.fdb_latexmk
*.fls
*.lof
*.log
*.lot
*.out
*.run.xml
*.synctex.gz
*.toc
missfont.log

# Prompts de redacción/revisión con IA (uso interno, no versionar)
**/PROMPT_*.md
PROMPT_*.md

# Interpretaciones AI de resultados (Fase 5, uso interno)
docs/fase5*_interpretacion.md
